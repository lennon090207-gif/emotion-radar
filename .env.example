# Copy to .env (or write to /root/.hermes/.env on the VPS) and fill in.
# Never commit the real .env.

APIFY_TOKEN=apify_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Vision analysis (Phase 2). Set ONE of these.
# OPENAI_API_KEY=sk-...
# OPENROUTER_API_KEY=sk-or-...

# Optional vision overrides.
# OPENAI_BASE_URL=https://openrouter.ai/api/v1
# VISION_MODEL=gpt-4o                # default for both passes when no per-pass override is set
# VISION_EVENT_MODEL=gpt-4o          # Pass 1 (visual event extraction) — use your strongest vision model here
# HOOK_STRATEGY_MODEL=gpt-4o         # Pass 2 (text-only strategy + mutations) — safe to point at a cheaper model

# Optional storage overrides.
# EMOTION_RADAR_DB=data/emotion_radar.db
# EMOTION_RADAR_DATA_DIR=data
